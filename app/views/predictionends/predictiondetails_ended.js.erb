window.endPrediction(<%= j @predictionend.actual_end_time %>, <%= j @predictionend.actual_end_price %>);

//console.log(<%= @prediction.id %>);
//replace the cancellation form on the left side with something abit more useful. Or maybe just remove the cancel button and add a line or 2.$('.stock-page-prediction-box').html("<%= j render @prediction %>").fadeIn("slow");


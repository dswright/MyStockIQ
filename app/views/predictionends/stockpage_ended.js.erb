window.removePrediction(); //remove the prediction from the graph.

$('.open-prediction-form-box').replaceWith("<%= j render 'shared/prediction_form' %>"); //replace the prediction with the prediction form.


//handle the tabbers on the loaded partial.
$( "#tabs" ).tabs();

$('#prediction-tab').click(function() {
$('#price').focus();
});

$('#comment-only-tab').click(function() {
$('#comment-only').focus();
});

$('.stream').prepend("<%= j render partial: 'streams/stream_main', locals: {streams: @streams}  %> ");

//var responses = $.parseHTML("<%= j raw @response %>");  //show the response messages.
//$('#response-box').html(responses).fadeIn("slow"); 
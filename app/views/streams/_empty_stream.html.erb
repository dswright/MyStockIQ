<% if streams.empty? %>
	<div class="comment-only-item" id="no_stream">
	        <p class="comment-only-item-main-txt">
				Be the first to make a prediction on this stock!
	        </p>
	  </div>
	</div>
<% end %>
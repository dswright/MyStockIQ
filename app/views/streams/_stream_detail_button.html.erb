<%= form_for(stream, remote: true) do |f| %>
  <%= hidden_field_tag 'stream_id', stream.id %>
  <%= f.submit "Show replies", class: "stream_detail_#{stream.streamable.id}", style: "float:left; width:100px;" %>
<% end %>
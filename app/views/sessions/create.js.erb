$('.signin-subtitle').after("<%= j render partial: 'shared/login_error_messages', locals: {object: @user} %>");
window.setTimeout(function() {
  $(".alert-container").fadeTo(500, 0).slideUp(500, function(){
    $(this).remove(); 
  });
}, 3000);
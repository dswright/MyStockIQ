<h1>Referral</h1>

<div class="referral_form">
	<%= form_for(@referral, remote: true) do |f| %>]
		<%= f.label :email  %>
	  	<%= f.email_field :email %><br />
	  	<%= f.submit "Send", class: "btn btn-primary", style: "float:left; width:50px;" %>
	<% end %>
</div>


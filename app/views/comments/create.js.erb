$('.prediction-post-comment:eq(0)').before("<%= j render partial: 'streams/stream_main', locals: {streams: @streams, nest_count: 0} %> ").fadeIn("slow");

var responses = $.parseHTML("<%= j raw @response %>");
$('#response-box').html(responses).fadeIn("slow");


$('.prediction-post-comment:eq(1)').before("<%= j render partial: 'streams/stream', locals: {stream_hash_array: @stream_hash_array}  %> ").fadeIn("slow");

var responses = $.parseHTML("<%= j raw @response %>");
$('#response-box').html(responses).fadeIn("slow");


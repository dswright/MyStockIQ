//var responses = $.parseHTML("<%= j raw @response %>");  //show the response messages. #raw is a ruby command that de-encrypts html
//$('#response-box').html(responses).fadeIn("slow"); 

alert("<%= j @response %>");

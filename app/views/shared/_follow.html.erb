<%= form_for(@current_user.active_relationships.build, remote: true) do |f| %>
  <%= hidden_field_tag :followed_id, @target.id %></div>
  <%= hidden_field_tag :followed_type, @target.class.name%>
  <%= f.submit "Follow", class: "btn" %>
<% end %>
<%= form_for(@current_user.active_relationships.build) do |f| %>
  <div><%= hidden_field_tag :followed_id, @target.id %></div>
  <div><%= hidden_field_tag :followed_type, @target.class.name%></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>